.documentList(ng-controller="DocumentListCtrl")
  ul
    li(ng-repeat="doc in documents")
      span.title
        a(href="document/{{doc.id}}")
          {{doc.title}}
      span.description
        {{doc.description}}
